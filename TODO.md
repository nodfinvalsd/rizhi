# TODO

记录 2026-09-12 界面改造（深色玻璃风）之后仍未完成的事项。

当前进度：窗口层 / 深色主题 / 左侧竖栏 / 知识列表卡片化 已完成并实测通过；
**本轮 10 个文件的改动尚未提交**。已完成项见文末，方便对照。

---

## 一、界面改造未完成

- [ ] **日程页重排**（优先级最高）
  目前只做了玻璃化（卡片换成半透明 + 1px 亮边），布局没动。
  计划：左侧加优先级色条、按 今天/明天/更早 分组。
  文件：`app/src/views/ScheduleView.vue`

- [ ] **图标系统**
  整个界面目前只有一个 📚 emoji，没有任何图标，玻璃风很吃图标的精致度。
  需要装 `@element-plus/icons-vue`，然后：侧栏三个菜单项、顶栏按钮、知识卡片元信息都配图标。

- [ ] **应用图标（任务栏 / 窗口 / 安装包）**
  现状：`app/electron/icon.ico` 只有 **1472 字节**（分辨率极低），
  且 `electron-builder.yml` 里 `signAndEditExecutable: false` 会让 Electron 跳过 rcedit，
  实际显示的是 **Electron 默认原子图标**。
  要做：准备 256×256 多尺寸 .ico，并解决 rcedit 被禁用的根因
  （yml 原注释：winCodeSign 解压需要系统支持创建符号链接，即需要开启 Windows 开发者模式）。

- [ ] **悬浮窗（widget）对齐新主题**
  `app/src/widget/WidgetApp.vue` 还是改造前的深色风格，与主窗口不统一。
  它已经是无边框 + 深色，是最容易先出效果的试验田，建议优先于日程页做。

- [ ] **弹窗 / 浮层逐个检查**
  已加全局玻璃规则（`.el-popper` / `.el-select__popper` / `.el-picker__popper` / `.el-dialog`），
  但没逐项验证：导入 .md 的拖拽上传区、分类管理树、`el-message` 消息提示、日期选择器面板。

- [ ] **窄窗口 / 小屏适配**
  当前窗口 1200×800，而本机屏幕只有 1280×800，几乎占满。
  左侧竖栏固定 200px，窄窗口下内容区会被压得很紧，没有响应式处理。

---

## 二、已发现但未修的缺陷

- [ ] **手动改 hash 会绕过路由守卫**
  `location.hash = ...` 时 URL 已经提交，`onBeforeRouteLeave / onBeforeRouteUpdate` 不触发。
  表现：编辑页有未保存内容时，地址栏变了但页面不换。
  Electron 应用没有地址栏，用户碰不到，优先级低。

- [ ] **`EditView` 的换 id 路径没有真实入口**
  已加 `watch(() => route.params.id)` + `onBeforeRouteUpdate` 守卫（防御性代码），
  但当前 UI 里不存在"编辑页 → 编辑页"的跳转，无法通过真实点击验证，只在程序触发下测过。

---

## 三、更早的遗留

- [ ] **项目文件夹改名**：`C:\D\个人知识库` → `C:\D\RiZhi`
  被占用句柄挡住（检测到 OpenCode.exe 等进程开着该目录）。
  步骤：关掉所有打开该目录的程序 → 改名 → **重建桌面快捷方式**
  （现有 `日知.lnk` 会指向不存在的路径）。

- [ ] **「所有日程」排序**：目前按开始时间倒序（最新在前），提过可能要改成正序，未确认。

- [ ] **今日总结的"回顾"入口**：后端已有 `GET /api/summary/recent?limit=`，
  前端没用上。可以做成"有记录的日期"列表，点进去回看往期总结。

---

## 四、工程 / 发布

- [ ] **代码签名**：`signAndEditExecutable: false` 是为绕开 winCodeSign 的符号链接问题，
  代价是 exe 没有版本信息、没有图标。要正式分发需要解决。
- [ ] **版本号**：`app/package.json` 仍是 `1.0.0`，多次重新打包安装都是同一版本号，不好追踪。
- [ ] **安装包内嵌数据库密码**：`app/electron/backend-env.json`（含 MySQL 密码）
  通过 `extraFiles` 打进安装包 —— 这是设计如此（本地单机应用），
  但**`app/release/` 下的产物不要外发**。
- [ ] **本轮改动未提交**：10 个文件，`git status` 可查。

---

## 五、已完成（本轮，供对照）

- [x] 窗口层：隐藏原生标题栏 + 英文菜单栏，改用 `titleBarStyle: 'hidden'` + `titleBarOverlay`
  实测 `WS_CAPTION` / `WS_THICKFRAME` 均保留 → 系统窗口按钮、圆角、投影、边缘缩放、贴边全部正常
- [x] 深色主题底座：Element Plus 深色变量 + 玻璃公式（`rgba(255,255,255,0.055)` + `blur(24px)` + 1px 亮边）+ 背景渐变与光晕
- [x] 骨架：48px 玻璃拖拽顶栏 + 200px 左侧竖栏（`el-menu` 换成 `router-link`，从根上消除"菜单项被折叠进 `···`"）
- [x] 知识列表：表格 → 行卡片；工具栏拆成"筛选 / 操作"两行
- [x] 知识详情：修掉正文白块（`MdPreview` 默认浅色主题；其深色主题是纯黑，已改为同色系玻璃）
- [x] 编辑器深色化：`MdEditor` / `MdPreview`（md-editor-v3）
- [x] 详情页：加载失败不再白屏（空状态 + 返回列表按钮）
- [x] 详情页 / 编辑页：换 `:id` 时内容会重载（Vue Router 复用组件实例导致 `onMounted` 不再触发）
- [x] 编辑页：未保存脏检查（离开前确认「留下 / 丢弃并离开」），保存后自动刷新快照
