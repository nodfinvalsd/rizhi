# 个人知识库实现方案

**文档版本：V4.0（极简个人版）**

**版本变更说明：**

- V1.0 → V2.0：定位为纯个人使用工具（单用户、本地部署）；删除用户中心/JWT/权限/提醒通知/定时任务；新增数据安全模块
- V2.0 → V3.0：产品形态改为桌面应用（托盘 + 悬浮窗 + 完整窗口），桌面壳 Tauri，后端保留 Spring Boot
- V3.0 → V4.0：全面精简。桌面壳 Tauri → Electron（免 Rust 工具链）；删除 Docker、单元测试、编辑历史、知识关联、周/月视图、自动备份、学习复习；AI/RAG 移入附录；第一版聚焦"记录 → 搜索 → 日程 → 备份"，够用即止

---

## 1. 项目定位

个人桌面工具：单用户、本地运行、无登录、无提醒。

形态：系统托盘常驻 + 快捷键呼出悬浮窗（快速记录 / 今日计划）+ 完整窗口（编辑、搜索、日程）。

核心目标：

> 随时快速记下知识，找得到，排得上日程，数据丢不了。

一句话原则：**个人够用即止，砍功能优先于加技术。**

## 2. 第一版功能范围

### 2.1 做（做完即用）

| 模块 | 功能 |
| ---- | ---- |
| 知识 | 创建 / 编辑 / 删除，Markdown 编辑，图片粘贴上传 |
| 组织 | 树形分类（一级为主），标签，收藏 |
| 搜索 | 标题 / 正文关键词搜索 + 分类 / 标签筛选 |
| 日程 | 创建 / 编辑 / 删除，今日 / 明日列表，状态勾选 |
| 桌面壳 | 托盘、悬浮窗（快速记录 + 今日计划）、完整窗口、全局快捷键 |
| 数据安全 | 单篇导出 .md、全库导出 zip、一键数据库备份 |

### 2.2 不做（以后想要再加）

```text
❌ 编辑历史 / 版本回滚
❌ 知识关联
❌ 学习复习（星级、复习卡片）
❌ 周视图 / 月视图 / 日历
❌ 定时任务 / 提醒
❌ AI / RAG（见附录 A，远期可选）
❌ 多用户 / 权限
❌ Docker、Redis、ES、MQ、测试框架
```

## 3. 技术栈

```text
桌面壳：Electron（托盘 / 悬浮窗 / 窗口管理 / 拉起后端）
前端：Vue 3 + Vite + Element Plus + Pinia
编辑器：md-editor-v3
后端：Java 21 + Spring Boot 4.x + MyBatis-Plus
数据库：MySQL 8.x
```

选型说明：

- 桌面壳从 Tauri 改为 Electron：免装 Rust 工具链，纯 JS 栈，开发最省事。代价是安装包约 100MB、内存占用更高，个人使用无所谓；
- MyBatis-Plus 用 3.5.15+ 的 `mybatis-plus-spring-boot4-starter`（Boot 3 的 starter 不兼容）；
- 不想装 MySQL 可换 H2 嵌入式数据库（附录 B），知识数据仍可随时导出 Markdown；
- 无 Spring Security / JWT、无 Docker、无测试框架（手动验证）。

## 4. 总体架构

```text
┌─────────────────────────────────┐
│          Electron 应用          │
│  ┌───────────────────────────┐  │
│  │ Vue 3 前端               │  │
│  │ （悬浮窗 / 完整窗口）      │  │
│  └─────────────┬─────────────┘  │
│   托盘 / 快捷键 / 主进程 spawn   │
└────────────────┼────────────────┘
                 │ HTTP/JSON（localhost:8900）
                 ↓
┌─────────────────────────────────┐
│ Spring Boot（应用启动时自动拉起）│
└────────────────┬────────────────┘
                 ↓
┌────────────────┬────────────────┐
│     MySQL      │  本地磁盘附件   │
└────────────────┴────────────────┘
```

- Electron 主进程负责：托盘、悬浮窗窗口、全局快捷键、启动 / 停止后端 jar；
- 前端只管调 localhost 接口，与壳无关，开发时可直接用浏览器调试。

## 5. 数据库设计（7 张表）

### 5.1 知识分类表

`t_knowledge_category`

```text
id  name  parent_id  sort  create_time  deleted
```

### 5.2 知识文档表

`t_knowledge`

```text
id  title  content  category_id  status  create_time  update_time  deleted
```

status：DRAFT / PUBLISHED

（摘要由正文自动截取，不单独存字段。）

### 5.3 标签表 / 关联表

`t_tag`：id name deleted

`t_knowledge_tag`：id knowledge_id tag_id

### 5.4 收藏表

`t_knowledge_favorite`：id knowledge_id create_time

### 5.5 附件表

`t_attachment`：id knowledge_id file_name file_path content_type create_time

附件存本地磁盘 `upload/yyyy/MM/`，库中只存路径。

### 5.6 日程表

`t_schedule`

```text
id  title  description  start_time  end_time  status  priority  create_time  update_time  deleted
```

status：TODO / DONE / CANCELLED

priority：LOW / MEDIUM / HIGH

## 6. 功能设计要点

### 6.1 快速记录（悬浮窗）

输入框回车即存：POST /api/knowledge，默认 DRAFT 无分类，之后在完整窗口补全。

### 6.2 Markdown 与图片

库存 Markdown 原文，前端 md-editor-v3 渲染；图片粘贴上传到本地磁盘，正文引用相对路径；导出时一并打包。

### 6.3 搜索

MySQL LIKE 实现标题 / 正文搜索 + 分类 / 标签筛选。个人量级（几千篇）完全够用，不用 ES。

### 6.4 日程

今日 / 明日两个列表视图 + 悬浮窗今日计划面板，勾选即完成。不做日历。

### 6.5 备份

```text
全库导出：GET /api/export → zip（全部 .md + 附件）
单篇导出：GET /api/knowledge/{id}/export → .md
一键备份：应用内"备份"按钮执行 mysqldump 到备份目录
```

## 7. 开发计划（三步）

| 步骤 | 内容 | 预计 |
| ---- | ---- | ---: |
| 第 1 步 | Spring Boot + 7 张表 + 知识 CRUD + 分类标签 + 搜索 | 3~5 天 |
| 第 2 步 | Electron 壳（托盘 / 悬浮窗 / 快捷键）+ 完整窗口前端 + 附件导出 | 4~6 天 |
| 第 3 步 | 日程 + 悬浮窗今日计划 + 备份 + 打磨 | 2~4 天 |

**第一版总计约 9~15 天。**

### 第 1 步清单

```text
□ Spring Boot 项目（Boot 4 + Java 21）
□ MyBatis-Plus（boot4 starter）+ MySQL
□ 7 张表建表脚本
□ 统一返回结构 + 异常处理
□ 知识 CRUD
□ 分类 / 标签
□ 搜索接口
```

### 第 2 步清单

```text
□ Electron + Vue 3 项目
□ 托盘图标 + 悬浮窗 + 完整窗口
□ 全局快捷键 Ctrl+Shift+Space
□ 主进程 spawn 后端 jar（启动 / 退出）
□ 完整窗口：列表页 / 编辑页 / 搜索页
□ md-editor-v3 集成 + 图片上传
□ 收藏
□ 单篇 / 全库导出
```

### 第 3 步清单

```text
□ 日程 CRUD
□ 今日 / 明日视图
□ 悬浮窗：快速记录 + 今日计划面板
□ 一键备份按钮
□ 手动走查全流程，修问题
```

## 8. 以后可选（想做再回来看）

```text
编辑历史 → 加 t_knowledge_version 表即可
知识关联 → 加 t_knowledge_relation 表即可
学习复习 → 加星级 + 下次复习时间字段即可
日历视图 → 前端换组件即可
AI / RAG → 附录 A
```

这些都是在现有表结构上"加一张表 / 加一个页面"的事，不需要推翻重来。

## 附录 A：AI / RAG 路线（远期参考）

知识库稳定后按需启动，无工期承诺：

```text
知识文档 → 切片 Chunk → Embedding（中文模型如 bge-m3）
→ 向量库 → 用户问题向量化 → Top-K 检索 → Prompt 组装 → LLM 回答（带来源引用）
```

注意：LLM / Embedding API 有调用费用，个人项目选性价比高的模型。

## 附录 B：换 H2 嵌入式数据库

如果不想在电脑上装 MySQL：依赖换成 H2（MySQL 兼容模式），数据存为本地文件，其余代码不动。备份靠 Markdown 导出即可。
